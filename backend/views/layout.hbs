<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{#if title}}{{title}} - {{/if}}Admin Panel - Movie</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-100">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar -->
        <aside class="w-64 bg-gray-800 text-white flex flex-col">
            <div class="p-6 border-b border-gray-700">
                <a href="/admin" class="text-xl font-bold flex items-center">
                    <i class="fas fa-film mr-2"></i>
                    Movie Admin
                </a>
            </div>
            
            <nav class="flex-1 p-4 space-y-2">
                <a href="/admin" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-700 transition {{#if isDashboard}}bg-gray-700{{/if}}">
                    <i class="fas fa-home mr-3 w-5"></i>
                    Dashboard
                </a>
                <a href="/admin/movies" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-700 transition {{#if isMovies}}bg-gray-700{{/if}}">
                    <i class="fas fa-film mr-3 w-5"></i>
                    Movies
                </a>
                <a href="/admin/users" class="flex items-center px-4 py-3 rounded-lg hover:bg-gray-700 transition {{#if isUsers}}bg-gray-700{{/if}}">
                    <i class="fas fa-users mr-3 w-5"></i>
                    Users
                </a>
            </nav>
            
            <div class="p-4 border-t border-gray-700">
                <div class="flex items-center px-4 py-2 mb-2">
                    <i class="fas fa-user-circle mr-3"></i>
                    <span class="text-sm">{{user.name}}</span>
                </div>
                <a href="/admin/logout" class="flex items-center px-4 py-2 rounded-lg hover:bg-gray-700 transition text-red-400">
                    <i class="fas fa-sign-out-alt mr-3"></i>
                    Logout
                </a>
            </div>
        </aside>
        
        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">
            <!-- Top Bar -->
            <header class="bg-white border-b border-gray-200 px-6 py-4">
                <h1 class="text-2xl font-bold text-gray-800">{{#if title}}{{title}}{{else}}Dashboard{{/if}}</h1>
            </header>
            
            <!-- Content Area -->
            <main class="flex-1 overflow-y-auto p-6">
                {{{body}}}
            </main>
        </div>
    </div>
</body>
</html>
